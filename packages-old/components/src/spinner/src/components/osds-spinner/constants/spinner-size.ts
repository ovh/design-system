enum ODS_SPINNER_SIZE {
    sm = 'sm',
    md = 'md',
    lg = 'lg',
}

const ODS_SPINNER_SIZES = Object.freeze(Object.values(ODS_SPINNER_SIZE));

export {
  ODS_SPINNER_SIZE,
  ODS_SPINNER_SIZES,
};
