import CheckedProperty from './properties/usage.checked.mdx';
import CheckingProperty from './properties/usage.checking.mdx';
import ColorProperty from './properties/usage.color.mdx';
import DisabledProperty from './properties/usage.disabled.mdx';
import HoverableProperty from './properties/usage.hoverable.mdx';
import InlineProperty from './properties/usage.inline.mdx';
import LoadingProperty from './properties/usage.loading.mdx';
import SizeProperty from './properties/usage.size.mdx';
import VariantProperty from './properties/usage.variant.mdx';
import RoundedProperty from './properties/usage.rounded.mdx';
import Slot from './slots/usage.slot.mdx';
import GenericStyle from '@ovhcloud/ods-common-core/docs/generic-style.mdx';
import { Canvas } from '@storybook/addon-docs';

<GenericStyle />

## Usage

### Default
<Canvas withSource="none">
  <osds-tile></osds-tile>
</Canvas >

```html
<osds-tile></osds-tile>
```

### Slot
<Slot />

### Inline
<InlineProperty />

### Hoverable
<HoverableProperty />

### Color
<ColorProperty />

### Size
<SizeProperty />

### Variant
<VariantProperty />

### Rounded
<RoundedProperty />

### Checked
<CheckedProperty />

### Checking
<CheckingProperty />

### Disabled
<DisabledProperty />

### Loading
<LoadingProperty />

### Tile with external click behaviour

```html
<osds-input type="number" placeholder="Placeholder" min="1" max="4" step="2" color="primary"></osds-input>
<osds-tile hoverable
               id="tile-1"
               onclick="handleTile1Click()"
               >
      Click me !
</osds-tile>

<script>
  const odsTile = document.querySelector('osds-tile');

  async function handleTile1Click() {
    logger.log('tile-1 click');
    // in case of enabled tile, toggle his checked state like a checkbox
    if (!odsTile.disabled) {
      odsTile.checked = !odsTile.checked;
    }
  }

  handleTile1Click();

  function myAsyncAction({checked, value}) {
    const max = 1, min = 0, delay = 1000;
    logger.log('simulate xhr call...', {checked, value});
    return new Promise(
      (resolve, reject) => {
          const isSuccess = Math.floor(Math.random() * (max - min + 1) + min)
          if (isSuccess) {setTimeout(() => {logger.log('xhr call success');resolve("backend updated")}, delay)}
          else {setTimeout(() => {logger.log('xhr call failed');reject(new Error("backend failed!"))}, delay)}
      }
    );
  }
</script>
```
