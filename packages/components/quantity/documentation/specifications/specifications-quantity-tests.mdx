### Unit
  **Render**
      - should render

  **Validation**
      - should display a warning message if no minus control is found
      - should display a warning message if no plus control is found
      - should display a warning message if no osds-input and vanilla input type number are found

### E2E
  **Render**
      - should render

  **Disabled**
    - should not be disabled by default
    - should be not clickable if disabled
    - controls and input should be disabled

  **stepDown**
    - should step down number of input when clicking on minus control
    - should trigger a disabled property on minus control when clicking on minus control step down to the min value

  **stepUp**
    - should step up number of input when clicking on plus control
    - should trigger a disabled property on plus control when clicking on plus control step up to the max value

### Screenshot
  **Render**
    - should render

  **Disabled**
    - should display disabled controls and input when disabled property is set on Quantity
