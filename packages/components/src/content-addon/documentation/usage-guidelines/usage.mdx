import GenericStyle from '@ovhcloud/ods-common-core/docs/generic-style.mdx';

<GenericStyle />

## Usage

### Default
#### Slot
Here is an example with all five slots but any slot(s) can be combined with another one to match your need:

<osds-content-addon>
  <span slot="start">Start</span>
  <span slot="main">Main</span>
  <span slot="end">End</span>
  <span slot="top">Top</span>
  <span slot="bottom">Bottom</span>
</osds-content-addon>

```html
<osds-content-addon>
  <span slot="start">Start</span>
  <span slot="main">Main</span>
  <span slot="end">End</span>
  <span slot="top">Top</span>
  <span slot="bottom">Bottom</span>
</osds-content-addon>
```
