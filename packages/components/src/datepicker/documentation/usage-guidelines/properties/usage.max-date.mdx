export const displayMaxDateDatepicker = () => {
    setTimeout(() => {
        const datepickerMaxDateUsage = document.querySelector('#max-datepicker');
        datepickerMaxDateUsage.maxDate = new Date();
    }, 0);
};

import { Canvas } from '@storybook/addon-docs';

<Canvas withSource="none">
  <div style={{ height: '300px' }}>
    <osds-datepicker id='max-datepicker' inline></osds-datepicker>
  </div>
</Canvas>

<>{ displayMaxDateDatepicker() }</>

```html
<osds-datepicker id='max-datepicker' inline></osds-datepicker>

<script>
  const datepickerMaxDateUsage = document.querySelector('#max-datepicker');
  datepickerMaxDateUsage.maxDate = new Date();
</script>
```
