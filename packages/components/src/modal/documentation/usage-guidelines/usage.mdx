import ColorProperty from './properties/usage.color.mdx';
import DismissibleProperty from './properties/usage.dismissible.mdx';
import HeadlineProperty from './properties/usage.headline.mdx';
import MaskedProperty from './properties/usage.masked.mdx';
import Slot from './slots/usage.slot.mdx';
import GenericStyle from '@ovhcloud/ods-common-core/docs/generic-style.mdx';

<GenericStyle />

## Usage

### Default
export const buttonTrigger = () => {
  setTimeout(() => {
    const toggleModal = document.getElementById('toggle-modal-1');
    const modal = document.getElementById('modal-doc');
    toggleModal.addEventListener('click', () => {
      modal.open();
    });
  }, 0);
};

<osds-button id="toggle-modal-1" color='primary' inline>Toggle modal</osds-button>

<osds-modal id="modal-doc" masked>Modal</osds-modal>

<>{ buttonTrigger() }</>

```html
<osds-modal>Modal</osds-modal>
```

### Slot
<Slot />

### Color
<ColorProperty />

### Dismissible
<DismissibleProperty />

### Headline
<HeadlineProperty />

### Masked
<MaskedProperty />
