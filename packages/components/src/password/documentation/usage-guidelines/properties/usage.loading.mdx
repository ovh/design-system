<div style={{ lineHeight: '0px' }}>
  <osds-password inline loading></osds-password>
</div>

```html
<osds-password inline loading></osds-password>
```

### Loading and disabled
<div style={{ lineHeight: '0px' }}>
  <osds-password inline loading disabled></osds-password>
</div>

```html
<osds-password inline loading disabled></osds-password>
```
