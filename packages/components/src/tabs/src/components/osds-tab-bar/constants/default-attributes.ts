import type { OdsTabBarAttribute } from '../interfaces/attributes';

import { ODS_TABS_SIZE } from '../../osds-tabs/constants/tabs-size';

const DEFAULT_ATTRIBUTE: OdsTabBarAttribute = Object.freeze({
  size: ODS_TABS_SIZE.md,
});

export {
  DEFAULT_ATTRIBUTE,
};
