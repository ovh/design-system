import { Canvas } from '@storybook/addon-docs';

<Canvas withSource="none">
  <osds-tile loading>Loading</osds-tile>
</Canvas >

`loading` is a functional attribute, not a visual one.

```html
<osds-tile loading>Loading</osds-tile>
```
