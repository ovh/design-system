---
title: "Migration 19.x to 20.x"
slug: ovhcloud-design-system-upgrade-migration-19-x-to-20-x
category: generic
type: overview
version: 19.7.3
tokens: 750
source: https://ovh.github.io/design-system/v19.7.3/llms/ovhcloud-design-system-upgrade-migration-19-x-to-20-x.txt
---

# Migration 19.x to 20.x

Version 20 adds support for [React 19](https://react.dev/blog/2024/12/05/react-19) **while keeping support for React 18**.

The peer dependency range is unchanged:

```json
"peerDependencies": {
  "react": ">=18.2.0",
  "react-dom": ">=18.2.0"
}
```

The public API of the components is **unchanged**: same props, same refs, same composition. There is **no code change required** on your side — whether you stay on React 18 or move to React 19.

## If you stay on React 18

Nothing to do. Installation, types and runtime keep working exactly as before.

## If you move to React 19

**1. Bump React and its types** to a matching major:

```shell
npm install react@^19 react-dom@^19
npm install -D @types/react@^19 @types/react-dom@^19
```

`@types/react` and `@types/react-dom` **must share the same major** (both `19`).

The React team ships codemods that automate most of the app-side migration — run them on your own code (they never need to touch ODS usage):

```shell
npx codemod@latest react/19/migration-recipe
npx types-react-codemod@latest preset-19 ./src
```

For anything beyond ODS, follow the official [React 19 upgrade guide](https://react.dev/blog/2024/04/25/react-19-upgrade-guide).

**2. Make sure you have a single `@types/react` version.** This is the one real pitfall — and it is **not** specific to ODS. If a transitive dependency still pulls `@types/react@18` while your app is on `19`, TypeScript reports errors such as:

```text
Type 'bigint' is not assignable to type 'ReactNode'.
'Xxx' cannot be used as a JSX component.
```

This is the classic "two copies of `@types/react`" conflict. Deduplicate to a single version:

```json
// npm — package.json
"overrides": {
  "@types/react": "^19",
  "@types/react-dom": "^19"
}

// yarn — package.json
"resolutions": {
  "@types/react": "^19",
  "@types/react-dom": "^19"
}

// pnpm — package.json
"pnpm": {
  "overrides": {
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
```

With pnpm you can also declare it in `pnpm-workspace.yaml` (top-level key, no `pnpm` wrapper):

```yaml
overrides:
  "@types/react": ^19
  "@types/react-dom": ^19
```

Then reinstall and verify a single version is resolved:

```shell
npm ls @types/react
# or: pnpm why @types/react
```

## Bundler & framework notes

These are unchanged by the React version — they apply the same way on React 18 and 19:

- **Vite**: works out of the box, CSS is handled natively.
- **Webpack 5**: configure `css-loader` (and `style-loader`) so the CSS shipped by ODS is bundled.
- **Next.js (App Router)**: Next 15 already requires React 19. Use ODS inside client components (`'use client'`). The Markdown component accesses `document` at load time, so import it through `next/dynamic` with `{ ssr: false }`.

## Need help?

If you hit an issue while upgrading, [open an issue on GitHub](https://github.com/ovh/design-system/issues) — especially if you see a type error that the `@types/react` deduplication above does not solve.
