import { OdsComponentAttributes } from '../../ods-component-attributes';

export interface OdsMenuAttributes extends OdsComponentAttributes {
  /**
   * Menu is disabled or not
   */
  disabled?: boolean;
}
