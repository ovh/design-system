import { ConfigTile } from '.';

export default {
  component: ConfigTile,
  title: 'ConfigTile dev',
};

export const CssModule = () => (
  <ConfigTile.CssModule />
);

export const Tailwind = () => (
  <ConfigTile.Tailwind />
);
