import { LocationTile } from '.';

export default {
  title: 'LocationTile dev',
};

export const CssModule = () => (
  <LocationTile.CssModule />
);

export const Tailwind = () => (
  <LocationTile.Tailwind />
);
