import { StatusModal } from '.';

export default {
  component: StatusModal,
  title: 'StatusModal dev',
};

export const CssModule = () => (
  <StatusModal.CssModule />
);

export const Tailwind = () => (
  <StatusModal.Tailwind />
);
