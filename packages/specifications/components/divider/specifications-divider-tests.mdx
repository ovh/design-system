**Color**
    - should have color to `default` by default

**Size**
    - should have `5` size by default

**Contrast**
    - should be contrasted if true
    - should not be contrasted by default

**Separator**
    - should displays the separator if set to true
    - should not display the separator by default
