- should display a `slot` with string content

**Level**
    - should have level flag to `body` by default

**Size**
    - should have size flag to `md` by default

**Color**
    - should have color flag to `default` by default

