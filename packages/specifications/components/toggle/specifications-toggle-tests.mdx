**color**
    - should be a `string`
    - should have color to `primary` by default
**Disabled**
    - should be a `boolean`
    - should not be disabled by default
    - should be not clickable if disabled
**checked**
    - should be a `boolean`
    - should have selected to `false` by default
    - should be modify css style if `true`
**checking**
    - should be a `boolean`
    - should have checking to `false` by default
