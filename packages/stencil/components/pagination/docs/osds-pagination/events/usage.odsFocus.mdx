```html
<osds-pagination id="myOdsPagination"> </osds-pagination>
```

```typescript
// vanilla typescript
const odsPagination = document.querySelector('osds-pagination#myOdsPagination');

odsPagination.addEventListener('odsFocus', event => {
  const evt = event as CustomEvent<void>;
  console.log('odsFocus event', evt.detail);
});
```
