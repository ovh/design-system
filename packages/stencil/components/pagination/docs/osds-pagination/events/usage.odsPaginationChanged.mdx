```html
<osds-pagination id="myOdsPagination"> </osds-pagination>
```

```typescript
// vanilla typescript
import { OdsPaginationCurrentChangeEvent } from '@ovhcloud/ods-core';

const odsPagination = document.querySelector('osds-pagination#myOdsPagination');

odsPagination.addEventListener('odsPaginationChanged', event => {
  const evt = event as OdsPaginationCurrentChangeEvent;
  console.log('odsPaginationChanged event', evt.detail);
});
```
