/**
 * default `ODS` configuration applied if there no custom configuration made on client side
 */
export const odsDefaultConfig = {
  id: Date.now(),
  logging: {
    active: false,
    color: true
  }
};
