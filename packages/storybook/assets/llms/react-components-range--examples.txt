---
title: "Range"
slug: range
category: component
type: examples
version: 19.7.3
tokens: 724
source: https://ovh.github.io/design-system/v19.7.3/llms/react-components-range--examples.txt
---

# Range

## Default

---

0100

0100

```jsx
<>
  <Range />
  <Range
    defaultValue={[
      50,
      75
    ]}
   />
</>
```

## Disabled

---

0100

0100

```jsx
<>
  <Range
    defaultValue={[
      20
    ]}
    disabled
  />
  <Range
    defaultValue={[
      50,
      75
    ]}
    disabled
  />
</>
```

## Min / Max

---

Max 500

0500

0500

Min 25

25100

25100

Max 75 & Min 25

2575

2575

```jsx
<>
  <p>
    Max 500  </p>
  <Range
    defaultValue={[
      50
    ]}
    max={500}
  />
  <Range
    defaultValue={[
      50,
      75
    ]}
    max={500}
  />
  <p>
    Min 25  </p>
  <Range
    defaultValue={[
      50
    ]}
    min={25}
  />
  <Range
    defaultValue={[
      50,
      75
    ]}
    min={25}
  />
  <p>
    Max 75 & Min 25  </p>
  <Range
    defaultValue={[
      50
    ]}
    max={75}
    min={25}
  />
  <Range
    defaultValue={[
      50,
      75
    ]}
    max={75}
    min={25}
  />
</>
```

## Step

---

0100

0100

```jsx
<>
  <Range
    defaultValue={[
      20
    ]}
    step={5}
  />
  <Range
    defaultValue={[
      50,
      75
    ]}
    step={5}
  />
</>
```

## Ticks

---

0100

0100

```jsx
<>
  <Range
    defaultValue={[
      20
    ]}
    ticks={[
      10,
      20,
      30,
      40,
      50,
      60,
      70,
      80,
      90
    ]}
  />
  <Range
    defaultValue={[
      50,
      75
    ]}
    ticks={[
      10,
      20,
      30,
      40,
      50,
      60,
      70,
      80,
      90
    ]}
  />
</>
```

## Custom Ticks

---

LowMediumHigh

0100

Very PoorPoorAverageGoodExcellent

```jsx
<>
  <Range ticks={[{
  label: 'Low',
  value: 25
}, {
  label: 'Medium',
  value: 50
}, {
  label: 'High',
  value: 75
}]} />
  <Range displayBounds={false} displayTooltip={false} max={5} min={1} ticks={[{
  label: 'Very Poor',
  value: 1
}, {
  label: 'Poor',
  value: 2
}, {
  label: 'Average',
  value: 3
}, {
  label: 'Good',
  value: 4
}, {
  label: 'Excellent',
  value: 5
}]} />
</>
```

## Controlled

---

This is a controlled component. The final value is only updated when the user releases the mouse button.

Final value:  
Dragged value:

0100

```jsx
const [draggingValue, setDraggingValue] = useState<number>();
  const [value, setValue] = useState<number>();
  function onDragging(detail: RangeValueChangeDetail) {
    setDraggingValue(detail.value[0]);
  }
  function onValueChange(detail: RangeValueChangeDetail) {
    setValue(detail.value[0]);
  }
  return <>
      <p>
        <span>Final value: {value}</span>
        <br />
        <span>Dragged value: {draggingValue}</span>
      </p>
      <Range onDragging={onDragging} onValueChange={onValueChange} value={draggingValue ? [draggingValue] : undefined} />
    </>;
}
```

## Form field

---

Range:

0100

```jsx
<FormField>
  <FormFieldLabel>
    Range:  </FormFieldLabel>
  <Range />
</FormField>
```

## Recipes

---

Range Input

0100
