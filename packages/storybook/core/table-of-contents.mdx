import { getLink } from './get-link.mdx';

export const getToC = (sectionArray) => (
  <ul class="sbdocs sbdocs-ul">
    {sectionArray.map((section, key) => (
      <li class="sbdocs sbdocs-li"
          key={key}>
        <strong>
          <a class="sbdocs sbdocs-a" href={getLink(section)}>
            {section}
          </a>
        </strong>
      </li>
      )
    )}
  </ul>
);
