import { Badge } from '@ovhcloud/ods-react';
import { Meta, Source } from '@storybook/blocks';
import * as AccordionStories from './accordion.stories';
import { Heading } from '../../../src/components/heading/Heading';

<Meta of={ AccordionStories } name="Migration v17 to v18" />

<Heading label="Accordion - migrate from v17 to v18" level={ 1 } />

<Heading label="Attributes changes" level={ 2 } />

`color` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

This design feature has been removed in order to match the new styling of the component.

`contrasted` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

This design feature has been removed in order to match the new styling of the component.

`disabled ` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `isDisabled` attribute to obtain the same behavior.

`opened ` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `isOpen` attribute to obtain the same behavior.

`size ` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

This design feature has been removed in order to match the new styling of the component.

<Heading label="Migration examples" level={ 2 } />

Without Color:

<Source code={`
<osds-accordion color='information'></osds-accordion>

<!-- is now -->

<ods-accordion></ods-accordion>
`}
        dark="true"
        language="html" />

Without Contrasted:

<Source code={`
<osds-accordion contrasted></osds-accordion>

<!-- is now -->

<ods-accordion></ods-accordion>
`}
        dark="true"
        language="html" />

Disabled:
<Source code={`
<osds-accordion disabled></osds-accordion>

<!-- is now -->

<ods-accordion is-disabled></ods-accordion>
`}
        dark="true"
        language="html" />

Opened:

<Source code={`
<osds-accordion opened></osds-accordion>

<!-- is now -->

<ods-accordion is-open></ods-accordion>
`}
        dark="true"
        language="html" />

Without Size:

<Source code={`
<osds-accordion size='md'></osds-accordion>

<!-- is now -->

<ods-accordion></ods-accordion>
`}
        dark="true"
        language="html" />
