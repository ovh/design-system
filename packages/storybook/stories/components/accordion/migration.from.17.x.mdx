import { Meta } from '@storybook/blocks';
import * as AccordionStories from './accordion.stories';

<Meta of={ AccordionStories } name='Migration From 17.x' />

# Accordion - migrate from v17 to v18
----

## Attributes changes

`color` <img src="https://img.shields.io/badge/removed-FF0000" />

Has been removed.

This design feature has been removed in order to match the new styling of the component.

`contrasted` <img src="https://img.shields.io/badge/removed-FF0000" />

Has been removed.

This design feature has been removed in order to match the new styling of the component.

`disabled ` <img src="https://img.shields.io/badge/updated-00FFFF" />

Has been updated.

You can use the new `isDisabled` attribute to obtain the same behavior.

`opened ` <img src="https://img.shields.io/badge/updated-00FFFF" />

Has been updated.

You can use the new `isOpen` attribute to obtain the same behavior.

`size ` <img src="https://img.shields.io/badge/removed-FF0000" />

Has been removed.

This design feature has been removed in order to match the new styling of the component.

## Migration examples

Without Color:
```html
<osds-accordion color='information'></osds-accordion>

<!-- is now -->

<ods-accordion></ods-accordion>
```

Without Contrasted:
```html
<osds-accordion contrasted></osds-accordion>

<!-- is now -->

<ods-accordion></ods-accordion>
```

Disabled:
```html
<osds-accordion disabled></osds-accordion>

<!-- is now -->

<ods-accordion is-disabled></ods-accordion>
```

Opened:
```html
<osds-accordion opened></osds-accordion>

<!-- is now -->

<ods-accordion is-open></ods-accordion>
```

Without Size:
```html
<osds-accordion size='md'></osds-accordion>

<!-- is now -->

<ods-accordion></ods-accordion>
```
