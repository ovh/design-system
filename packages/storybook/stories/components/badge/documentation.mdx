import { Canvas, Meta, Markdown } from '@storybook/blocks';
import SpecificationsBadge from '@ovhcloud/ods-components/src/components/badge/documentation/spec.md?raw';
import { Banner } from '../../banner';
import { DocNavigator } from '../../doc-navigator';
import * as BadgeStories from './badge.stories';
import { LINK_ID } from '../../zeroheight';

<Meta of={ BadgeStories } name='Documentation' />

<Banner of={ BadgeStories } />

# Overview

Badge show concise metadata or the current status for an item, in a compact format. They are used inline, next to the title or text they complete:

<Canvas of={ BadgeStories.Default } sourceState='none' />

<DocNavigator of={ BadgeStories } linkId={ LINK_ID.BADGE } />

<Markdown>{ SpecificationsBadge }</Markdown>

# Style customization

You can add your own style on the badge element using the part `badge`.

Custom badge css:

<Canvas of={ BadgeStories.CustomCSS } sourceState='shown'/>

# Examples

## Default

<Canvas of={ BadgeStories.Default } sourceState='shown' />

## Color

<Canvas of={ BadgeStories.Color } sourceState='shown' />

## Shape

<Canvas of={ BadgeStories.Shape } sourceState='shown' />

## Size

<Canvas of={ BadgeStories.Size } sourceState='shown' />
