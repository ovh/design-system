import { Meta } from '@storybook/blocks';
import { Banner } from '../../../src/components/banner/Banner';
import { Canvas } from '../../../src/components/canvas/Canvas';
import { Heading } from '../../../src/components/heading/Heading';
import * as BreadcrumbStories from './breadcrumb.stories';

<Meta of={ BreadcrumbStories } name="Examples" />

<Banner of={ BreadcrumbStories } />

<Heading label="Default" level={ 2 } />

<Canvas of={ BreadcrumbStories.Default } sourceState="shown" />

<Heading label="Collapsed" level={ 2 } />

<Canvas of={ BreadcrumbStories.Collapsed } sourceState="shown" />

You can customize the number of item displayed before and after the ellipsis.

<Canvas of={ BreadcrumbStories.CustomAfterBeforeCollapse } sourceState="shown" />

You can also customize the threshold from when the breadcrumb will collapse.

<Canvas of={ BreadcrumbStories.CustomCollapseThreshold } sourceState="shown" />

Or you can even deactivate the whole collapse logic.

<Canvas of={ BreadcrumbStories.NoCollapse } sourceState="shown" />
