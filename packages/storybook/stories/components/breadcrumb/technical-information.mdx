import { Canvas, Meta } from '@storybook/blocks';
import SpecificationsBreadcrumb from '@ovhcloud/ods-components/src/components/breadcrumb/documentation/custom-elements.json';
import { Banner } from '../../../src/components/banner/Banner';
import { Heading } from '../../../src/components/heading/Heading';
import { StorybookLink } from '../../../src/components/storybookLink/StorybookLink';
import { TechnicalSpecification } from '../../../src/components/technicalSpecification/TechnicalSpecification';
import { ODS_COMPONENTS_TITLE, STORY } from '../../../src/constants/meta';
import * as BreadcrumbStories from './breadcrumb.stories';

<Meta of={ BreadcrumbStories } name="Technical information" />

<Banner of={ BreadcrumbStories } />

<Heading label="Overview" level={ 2 } />

<Canvas of={ BreadcrumbStories.Overview } sourceState="none" />

<TechnicalSpecification data={ SpecificationsBreadcrumb } />

<Heading label="Style customization" level={ 2 } />

The breadcrumb component wraps and applies style on the breadcrumb items contained inside.

You can directly add CSS classes on each item that you want to customize.

If you want to go further and customize the items directly, you can use the part `link` or `last`.

Custom CSS :

<Canvas of={ BreadcrumbStories.CustomCSS } sourceState="shown" />

<Heading label="Examples" level={ 2 } />

<Heading label="Default" level={ 3 } />

<Canvas of={ BreadcrumbStories.Default } sourceState="shown" />

<Heading label="Collapsed" level={ 3 } />

<Canvas of={ BreadcrumbStories.Collapsed } sourceState="shown" />

<Heading label="Get the clicked item" level={ 3 } />

If you need to perform specific actions when a breadcrumb item is clicked,
you can observe the `odsBreadcrumbItemClick` event, that will return the target of the click.

<Canvas of={ BreadcrumbStories.ClickedItem } sourceState="shown" />

<Heading label="Breadcrumb item attributes" level={ 3 } />

Each breadcrumb item supports the same attribute list as the <StorybookLink kind={ ODS_COMPONENTS_TITLE.link } label="ods-link" story={ STORY.technicalInformation } /> component.

<Canvas of={ BreadcrumbStories.CustomItem } sourceState="shown" />
