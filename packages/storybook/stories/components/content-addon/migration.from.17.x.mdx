import { OdsBadge } from '@ovhcloud/ods-components/react';
import { Meta, Source } from '@storybook/blocks';
import { Heading } from '../../../src/components/heading/Heading';

<Meta title="ODS Components/Content Addon" />

<Heading label="Content Addon - migrate from v17 to v18" level={ 1 } />

Content Addon has been removed from ODS components. <OdsBadge color="critical" label="Removed" size="sm" />

The same behaviour can be achieved with simple CSS.

Let's take an example of the content-addon usage:

<Source code={`
<osds-content-addon>
  <span slot="top">top</span>
  <span slot="start">start</span>
  <span slot="main">main</span>
  <span slot="end">end</span>
  <span slot="bottom">bottom</span>
</osds-content-addon>
`}
        dark="true"
        language="html" />

If we observe closely, we can see that both `top` and `bottom` element doesn't even need any specific style
as they follow the natural rendering workflow (vertical from top to bottom).

Let's focus then on the middle part, you can for example use `grid` to reproduce the same three columns layout:

<Source code={`
<style>
  .grid {
    display: grid;
    grid-template-columns: auto auto auto;
  }
</style>

<div>top</div>
<div class='grid'>
  <span>start</span>
  <span>main</span>
  <span>end</span>
</div>
<div>bottom</div>
`}
        dark="true"
        language="html" />

Or you can also use `flex-flow` if you have a dynamic amount of columns:

<Source code={`
<style>
  .flex {
    display: flex;
    flex-flow: row;
  }
</style>

<div>top</div>
<div class="grid">
  <span>start</span>
  <span>main 1</span>
  <span>main 2</span>
  <span>end</span>
</div>
<div>bottom</div>
`}
        dark="true"
        language="html" />
