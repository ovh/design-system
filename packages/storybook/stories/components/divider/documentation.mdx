import { Markdown, Meta } from '@storybook/blocks';
import { Banner } from '../../banner';
import * as DividerStories from './divider.stories';
import { ATOMIC_TYPE, IdentityCard } from '../../identity-card';

<Meta of={ DividerStories } name='Documentation' />

<Banner of={ DividerStories } />

_A **Divider** component is a spacer used to add white space between two elements on a page._

## Overview

<div className="documentation-overview">
  <div>
    A **Divider** is a UI component used to separate content into clear, distinct sections, providing visual breaks
    between different elements or groups of content.
  </div>

  <IdentityCard aliases={ ['Separator', 'Spacer', 'Horizontal Line'] }
                atomicType={ ATOMIC_TYPE.quark }
                className="documentation-overview__table"
                figmaLink="https://www.figma.com/proto/tIKzHa5KvHHyosgIgyBswB/Design-Tokens"
                githubUrl="https://github.com/ovh/design-system/tree/master/packages/ods/src/components/divider"
                name="Divider"
                relatedComponents={ [] } />
</div>

## Anatomy

![Component anatomy](components/divider/anatomy.png "Component anatomy")

1. A white space
2. A styled, horizontal line separator
3. A second white space, with equal size to the first one

## Usage

**Divider** is used to separate content on a page.

It brings clarity to a layout by dividing content in proximity.

### Dos & Don'ts

<Markdown className="documentation-do-table">
  { `
| ✅ Do                                                                                                                                                                           |
|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| - Use **Divider** to separate two elements.                                                                                                                                    |
| - Use a **Divider** as a substitute for styled semantic sectioning HTML (for example borders).                                                                           |
| - A **Divider** with a line separator might be used below a header / a section title.                                                                                          |
` }
</Markdown>

<Markdown className="documentation-dont-table">
  { `
| ❌ Don't                                                                                                                                                                          |
|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| - Use a **Divider** with a line separator above a header / a section title.                                                                                                |
| - Use **Divider** sparingly on a page.                                                                                                                                           |
| - Overuse **Divider** on a page, so it won't create unnecessary noise.                                                                                                     |
` }
</Markdown>

## Placement

The **Divider** has to be used to create a separation between two elements and to separate the contents of a page.

The **Divider** can also be used inside components to create separation between a set of sections.

## Behavior

N/A

## Variation

The horizontal line separator has a fixed height of 1 pixel.
 `Space` around **Divider** can be defined in a referential from "0" to "10", depending on the spacing required between two elements.

## Accessibility

N/A
