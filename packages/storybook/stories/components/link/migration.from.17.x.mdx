import { OdsBadge } from '@ovhcloud/ods-react';
import { Meta, Source } from '@storybook/blocks';
import * as LinkStories from './link.stories';
import { Heading } from '../../../src/components/heading/Heading';

<Meta of={ LinkStories } name="Migration From 17.x" />

<Heading label="Link - migrate from v17 to v18" level={ 1 } />

<Heading label="Attributes changes" level={ 2 } />

`contrasted` <OdsBadge color="critical" size="sm">Removed</OdsBadge>

Has been removed.

You can use the style customization to achieve the same result, until a new color get validated by the design team.

`disabled` <OdsBadge color="information" size="sm">Updated</OdsBadge>

Has been updated.

You can use the new `isDisabled` attribute to obtain the same behavior.

`slot ` <OdsBadge color="critical" size="sm">Removed</OdsBadge>

Has been removed.

You can no longer put any content inside the component. The ods-link now enforces the rendering of a label, and eventually an icon.
You can use the new attributes `label` and `icon` to achieve that.

<Heading label="Migration examples" level={ 2 } />

Label link:

<Source code={`
<osds-link href="/page">
  My link
</osds-link>

<!-- is now -->

<ods-link href="/page" label="Content"></ods-link>
`}
        dark="true"
        language="html" />

Disabled link:

<Source code={`
<osds-link href="/page" disabled>
  My link
</osds-link>

<!-- is now -->

<ods-link href="/page" label="My link" is-disabled>
</ods-link>
`}
        dark="true"
        language="html" />

Icon link:

<Source code={`
<osds-link href="/page">
  <osds-icon name="warning" size="xs"></osds-icon>
</osds-link>

<!-- is now -->

<ods-link href="/page" icon="triangle-exclamation"></ods-link>
`}
        dark="true"
        language="html" />
