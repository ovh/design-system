import { Canvas, Meta, Markdown } from '@storybook/blocks';
import SpecificationsModal from '@ovhcloud/ods-components/src/components/modal/documentation/spec.md?raw';
import { Banner } from '../../banner';
import { DocNavigator } from '../../doc-navigator';
import * as ModalStories from './modal.stories';
import { LINK_ID } from '../../zeroheight';

<Meta of={ ModalStories } name='Documentation' />

<Banner of={ ModalStories } />

# Overview

A Modal component is used to overlay with important content the main view of a site and block interactions with it.

<Canvas of={ ModalStories.Overview } sourceState='none' />

<DocNavigator of={ ModalStories } linkId={ LINK_ID.MODAL } />

<Markdown>{ SpecificationsModal }</Markdown>

# Style customization

You can update the modal's content by directly updating the slot elements. You can also update it using the `::part(dialog)`.

Custom modal css:

<Canvas of={ ModalStories.CustomCSS } sourceState='shown'/>

# Examples

## Default

<Canvas of={ ModalStories.Default } sourceState='shown' />

## Not Dismissible

<Canvas of={ ModalStories.NotDismissible } sourceState='shown' />

## Color

<Canvas of={ ModalStories.Color } sourceState='shown' />

## Actions slot

<Canvas of={ ModalStories.Actions } sourceState='shown' />
