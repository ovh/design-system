import { Canvas, Meta, Markdown } from '@storybook/blocks';
import SpecificationsPhoneNumber from '@ovhcloud/ods-components/src/components/phone-number/documentation/spec.md?raw';
import { Banner } from '../../banner';
import { DocNavigator } from '../../doc-navigator';
import * as PhoneNumberStories from './phone-number.stories';
import { LINK_ID } from '../../zeroheight';

<Meta of={ PhoneNumberStories } name='Documentation' />

<Banner of={ PhoneNumberStories } />

# Overview

Phone Field component is a combo of a selection of country phone indicator and an input field for entering a phone number:

<Canvas of={ PhoneNumberStories.Overview } sourceState='none' />

<DocNavigator of={ PhoneNumberStories } linkId={ LINK_ID.PHONE_NUMBER } />

<Markdown>{ SpecificationsPhoneNumber }</Markdown>

# Style customization

You can add your own style on the country selector element using the part `select` and on
the input element using the part `input`.

Please note that current select component does not provide a high level of style customization, if you encounter
a situation where it prevents you yo use the phone-number component, please reach out to us.

Custom phone-number css:

<Canvas of={ PhoneNumberStories.CustomCSS } sourceState='shown'/>

# Examples

## Default

<Canvas of={ PhoneNumberStories.Default } sourceState='shown' />

## Clearable

<Canvas of={ PhoneNumberStories.Clearable } sourceState='shown' />

## Disabled

<Canvas of={ PhoneNumberStories.Disabled } sourceState='shown' />

## Error

<Canvas of={ PhoneNumberStories.Error } sourceState='shown' />

## Loading

<Canvas of={ PhoneNumberStories.Loading } sourceState='shown' />

## Pattern

With this pattern setting, the component will only allow for numeric characters:

<Canvas of={ PhoneNumberStories.Pattern } sourceState='shown' />
