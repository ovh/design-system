import { Canvas, Meta, Markdown } from '@storybook/blocks';
import SpecificationsPhoneNumber from '@ovhcloud/ods-components/src/components/phone-number/documentation/custom-elements.json';
import { Banner } from '../../../src/components/banner/Banner';
import { TechnicalSpecification } from '../../../src/components/technicalSpecification/TechnicalSpecification';
import * as PhoneNumberStories from './phone-number.stories';
import { PhoneNumber as FormFieldPhoneNumber } from '../form-field/form-field.stories';

<Meta of={ PhoneNumberStories } name='Technical information' />

<Banner of={ PhoneNumberStories } />

## Overview

<Canvas of={ PhoneNumberStories.Overview } sourceState='none' />

<TechnicalSpecification data={ SpecificationsPhoneNumber } />

## Style customization

You can add your own style on the country selector element using the part `select` and on
the input element using the part `input`.

Please note that current select component does not provide a high level of style customization, if you encounter
a situation where it prevents you yo use the phone-number component, please reach out to us.

Custom CSS:

<Canvas of={ PhoneNumberStories.CustomCSS } sourceState='shown'/>

## Examples

### Default

<Canvas of={ PhoneNumberStories.Default } sourceState='shown' />

### Clearable

<Canvas of={ PhoneNumberStories.Clearable } sourceState='shown' />

### Disabled

<Canvas of={ PhoneNumberStories.Disabled } sourceState='shown' />

### Error

<Canvas of={ PhoneNumberStories.Error } sourceState='shown' />

### Loading

<Canvas of={ PhoneNumberStories.Loading } sourceState='shown' />

### Locale

For further explanation about locale strategy, see [the "Locale" behavior](?path=/docs/ods-components-form-elements-phone-number--documentation#locale)

<Canvas of={ PhoneNumberStories.Locale } sourceState='shown' />

### ISO code

For further explanation about ISO code strategy, see [the "Iso Code" behavior](?path=/docs/ods-components-form-elements-phone-number--documentation#iso-code)

<Canvas of={ PhoneNumberStories.ISOCode } sourceState='shown' />

### Pattern

With this pattern setting, the component will only allow for numeric characters:

<Canvas of={ PhoneNumberStories.Pattern } sourceState='shown' />

### Readonly

<Canvas of={ PhoneNumberStories.Readonly } sourceState='shown' />

### Included in a [**Form Field**](?path=/docs/ods-components-form-elements-form-field--technical-information)
<Canvas of={ FormFieldPhoneNumber } sourceState='shown' />
