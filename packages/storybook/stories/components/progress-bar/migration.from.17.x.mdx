import { Badge } from '@ovhcloud/ods-react';
import { Meta, Source } from '@storybook/blocks';
import * as ProgressBarStories from './progress-bar.stories';
import { Heading } from '../../../src/components/heading/Heading';

<Meta of={ ProgressBarStories } name="Migration v17 to v18" />

<Heading label="Progress Bar - migrate from v17 to v18" level={ 1 } />

<Heading label="Slot changes" level={ 2 } />

`end` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

You can now add any text directly after the component instead of using the slot.

<Heading label="Migration examples" level={ 2 } />

Slot end progress bar:

<Source code={`
<osds-progress-bar value="50">
  <span slot="end">50%</span>
</osds-progress-bar>

<!-- is now -->

<ods-progress-bar value="50"></ods-progress-bar><span>50%</span>
`}
        dark="true"
        language="html" />
