import { Meta } from '@storybook/blocks';
import * as RangeStories from './range.stories';

<Meta of={ RangeStories } name='Migration From 17.x' />

# Range - migrate from v17 to v18
----

## Attributes changes

`color` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

This design feature has been removed in order to match the new styling of the component.

`disabled` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `isDisabled` attribute to obtain the same behavior.

`error` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `hasError` attribute to obtain the same behavior.

`errorStateControl` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Was not relevant for few versions already.

`formControl` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Was not relevant for few versions already.

`forbidden-values` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

You can replace it with a regex on the `pattern` attributes.

`inline` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Range is now `inline-block` by default.
You can add some style directly on the component if you want to override that.

## Migration examples

Disabled range:
```html
<osds-range disabled>
</osds-range>

<!-- is now -->

<ods-range is-disabled>
</ods-range>
```

Error range:
```html
<osds-range error>
</osds-range>

<!-- is now -->

<ods-range has-error>
</ods-range>
```

Inline range:
```html
<osds-range inline>
</osds-range>

<!-- is now -->

<ods-range>
</ods-range>
```
