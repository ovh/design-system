import { Canvas, Meta, Markdown } from '@storybook/blocks';
import SpecificationsSwitch from '@ovhcloud/ods-components/src/components/switch/documentation/spec.md?raw';
import { Banner } from '../../banner';
import { DocNavigator } from '../../doc-navigator';
import * as SwitchStories from './switch.stories';
import { LINK_ID } from '../../zeroheight';

<Meta of={ SwitchStories } name='Documentation' />

<Banner of={ SwitchStories } />

# Overview

Switch component is used to switch between up-to-4 states, actions or options:

<Canvas of={ SwitchStories.Default } sourceState='none' />

<DocNavigator of={ SwitchStories } linkId={ LINK_ID.SWITCH } />

<Markdown>{ SpecificationsSwitch }</Markdown>

# Examples

## Default

<Canvas of={ SwitchStories.Default } sourceState='shown' />

## Checked
<Canvas of={ SwitchStories.Checked } sourceState='shown' />

## Disabled
<Canvas of={ SwitchStories.Disabled } sourceState='shown' />

## Size
<Canvas of={ SwitchStories.Size } sourceState='shown' />


