import { Badge } from '@ovhcloud/ods-react';
import { Meta } from '@storybook/blocks';
import * as TableStories from './table.stories';
import { Heading } from '../../../src/components/heading/Heading';

<Meta of={ TableStories } name="Migration From 17.x" />

<Heading label="Table - migrate from v17 to v18" level={ 1 } />

<Heading label="Attributes changes" level={ 2 } />

`variant ` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

A new `default` value has been added, it is the default value if no variant are set,
so you don't need to change anything from your current integration.
