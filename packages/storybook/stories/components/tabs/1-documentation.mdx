import { Canvas, Markdown, Meta } from '@storybook/blocks';
import { Banner } from '../../banner';
import * as TabsStories from './tabs.stories';
import { OdsIcon } from '@ovhcloud/ods-components/react';
import { ODS_ICON_NAME } from '@ovhcloud/ods-components';

<Meta of={ TabsStories } name='Documentation' />

<Banner of={ TabsStories } />

_**Tabs** are a way of navigating between multiple panels, reducing clutter and fitting more into a smaller space_

<Canvas of={ TabsStories.Overview } sourceState='none' />

## Overview
<div>
  <div id='description'>
    **Tabs** are used to organize content by grouping similar information on the same page.
  </div>
  <table>
    <tbody>
    <tr>
      <th scope="row">Name</th>
      <td><strong>Tabs</strong></td>
    </tr>
    <tr>
      <th scope="row">Also known as</th>
      <td><em>Tab Navigation, Tabbed interface</em></td>
    </tr>
    <tr>
      <th scope="row">Atomic type</th>
      <td>
        <a href='https://atomicdesign.bradfrost.com/chapter-2/#molecules'>Molecule<OdsIcon
          name={ ODS_ICON_NAME.externalLink }
          style={ { fontSize: '.7rem', paddingLeft: '.3rem', paddingRight: '.2rem' } }></OdsIcon></a>
      </td>
    </tr>
    <tr>
      <th scope="row">Related component(s)</th>
      <td>
        [**Text**](?path=/docs/ods-components-content-tag--documentation),
        [**Link**](?path=/docs/ods-components-actions-link--documentation)
      </td>
    </tr>
    <tr>
      <th scope="row">Links</th>
      <td>
        <a href='https://www.figma.com/proto/tIKzHa5KvHHyosgIgyBswB/Design-Tokens'>Design<OdsIcon
          name={ ODS_ICON_NAME.externalLink }
          style={ { fontSize: '.7rem', paddingLeft: '.3rem', paddingRight: '.2rem' } }></OdsIcon></a><br />
        <a href='https://github.com/ovh/design-system/tree/master/packages/components/src/tabs'>Github<OdsIcon
          name={ ODS_ICON_NAME.externalLink }
          style={ { fontSize: '.7rem', paddingLeft: '.3rem', paddingRight: '.2rem' } }></OdsIcon></a>
      </td>
    </tr>
    </tbody>
  </table>
</div>

## Anatomy

![Component anatomy](components/tabs/anatomy.png "Component anatomy")

1. Active tab
2. Unselected tab

## Usage

**Tabs** component is ideal for dashboards, settings pages, profile sections, and any interface where multiple views need to be accessible from the same page.

They can also tend to be used to filter content via some common denominator.

### Dos & Don'ts
<Markdown>
  { `
| ✅ Do                                                                                                                                                                           |
|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| - Group data in sub-navigation with **Tabs**                                                                                                                                   |
` }
</Markdown>
<Markdown>
  { `
| ❌ Don't                                                                                                                                                                          |
|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| - Use a large amount of **Tabs**, as it can be confusing for the user                                                                                                            |
| - Use long labelled **Tabs**                                                                                                                                                     |
| - Use conjugated verbs in labels                                                                                                                                                 |
| - Use external [**Link**](?path=/docs/ods-components-actions-link--documentation) as **Tabs**                                                                                    |
| - Avoid elements comparison in multiple **Tabs** (context switching)                                                                                                             |
| - Avoid nesting **Tabs** in a web page                                                                                                                                           |
| - Use **Tabs** as a process progression                                                                                                                                          |
` }
</Markdown>

## Placement

**Tabs** are **often used in the top part of a web page**, as it can act as a navigation focus.

They are **left-centered by default**.

Each **Tab** has an automatic width based on the content's length.

## Behavior

**By default, one of the **Tabs** is always selected**.

**Tabs** can be hovered, focused and selected. A **Tab** can also act as a disabled **Tab**.

All **Tabs** stay always on a same line, and can be scrolled if they don't fit in their container.

When the selected **Tab** changes, an event is triggered.

Each **Tab** has a panel displaying free content. Selecting a **Tab** displays the corresponding panel.

The panel does not depend on the **Tab** component, it is in the developer’s hands.

## Variants

N/A

## Accessibility

All inner **Tabs** follow the same focusing/keyboard navigation as [**Button**](?path=/docs/ods-components-actions-button--documentation)
