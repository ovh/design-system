import { Canvas, Meta, Markdown } from '@storybook/blocks';
import SpecificationsTabs from '@ovhcloud/ods-components/src/components/tabs/documentation/spec.md?raw';
import { Banner } from '../../banner';
import { DocNavigator } from '../../doc-navigator';
import * as TabsStories from './tabs.stories';
import { LINK_ID } from '../../zeroheight';

<Meta of={ TabsStories } name='Documentation' />

<Banner of={ TabsStories } />

# Overview

Tabs are a way of navigating between multiple panels, reducing clutter and fitting more into a smaller space:

<Canvas of={ TabsStories.Overview } sourceState='none' />

<DocNavigator of={ TabsStories } linkId={ LINK_ID.TABS } />

<Markdown>{ SpecificationsTabs }</Markdown>

# Style customization

You can apply your own style on the whole tabs container or on each tab using css classes.

Custom tabs css:

<Canvas of={ TabsStories.CustomCSS } sourceState='shown'/>

# Examples

## Default

<Canvas of={ TabsStories.Default } sourceState='shown' />

## Disabled tab

<Canvas of={ TabsStories.Disabled } sourceState='shown' />

## Selected tab

<Canvas of={ TabsStories.Selected } sourceState='shown' />
