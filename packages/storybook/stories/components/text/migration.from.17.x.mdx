import { OdsBadge } from '@ovhcloud/ods-components/react';
import { Meta, Source } from '@storybook/blocks';
import * as TextStories from './text.stories';
import { Heading } from '../../../src/components/heading/Heading';

<Meta of={ TextStories } name="Migration From 17.x" />

<Heading label="Text - migrate from v17 to v18" level={ 1 } />

<Heading label="Attributes changes" level={ 2 } />

`breakSpaces` <OdsBadge color="critical" label="Removed" size="sm" />

`color` <OdsBadge color="critical" label="Removed" size="sm" />

`contrasted` <OdsBadge color="critical" label="Removed" size="sm" />

`level` <OdsBadge color="critical" label="Removed" size="sm" />

`size` <OdsBadge color="critical" label="Removed" size="sm" />

Have been removed

You can use the new `preset` attribute to obtain the same rendering.

<Heading label="Migration examples" level={ 2 } />

Size & Level text:

<Source code={`
<osds-text size="500" level="heading">My text</osds-text>

<!-- is now -->

<ods-text preset="heading-1">My text</ods-text>
`}
        dark="true"
        language="html" />

Color text:

<Source code={`
<osds-text color="primary">My text</osds-text>

<!-- is now -->

<ods-text class="my-text">My text</ods-text>

<style>
  .my-text::part(text) {
    color: var(--ods-color-primary-500);
  }
</style>
`}
        dark="true"
        language="html" />

breakSpaces text:

<Source code={`
<osds-text break-spaces>My text</osds-text>

<!-- is now -->

<ods-text class="my-text">My text</ods-text>

<style>
  .my-text::part(text) {
    white-space: break-spaces;
  }
</style>
`}
        dark="true"
        language="html" />
