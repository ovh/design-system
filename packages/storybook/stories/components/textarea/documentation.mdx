import { Canvas, Markdown, Meta } from '@storybook/blocks';
import { Banner } from '../../banner';
import * as TextareaStories from './textarea.stories';
import { ATOMIC_TYPE, IdentityCard } from '../../identity-card';

<Meta of={ TextareaStories } name='Documentation' />

<Banner of={ TextareaStories } />

_A **Textarea** component allows users to input and edit multiple lines of text._

<Canvas of={ TextareaStories.Overview } sourceState='none' />

## Overview

<div className="documentation-overview">
  <div>
    The **Textarea** component is used to capture and display multi-line text input from users.

    It is typically used in forms where users need to provide detailed information, comments, or descriptions.
  </div>

  <IdentityCard aliases={ ['Text Box'] }
                atomicType={ ATOMIC_TYPE.atom }
                className="documentation-overview__table"
                figmaLink="https://www.figma.com/proto/tIKzHa5KvHHyosgIgyBswB/Design-Tokens"
                githubUrl="https://github.com/ovh/design-system/tree/master/packages/ods/src/components/textarea"
                name="Textarea"
                relatedComponents={ [{ name: 'Tag' }] } />
</div>

## Anatomy

![Component anatomy](components/textarea/anatomy.png "Component anatomy")

1. **Placeholder** : provide hints or example of what to type
2. **Main container**
3. **Resize native icon** (optional)
4. **Scrollbar** : when filled content is longer than its container (optional)

## Usage

Use the **Textarea** component to allow users to enter multi-line text in forms, such as comments, messages, descriptions, or other detailed information.

Ensure that the **Textarea** is appropriately sized for the expected input and provides clear guidance on what information is needed.

### Dos & Don'ts

<Markdown className="documentation-do-table">
  { `
| ✅ Do                                                                                                                                                                           |
|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| - Use **Textarea** for a text longer than a single line.                                                                                                                       |
| - Use a [**Form Field**](?path=/docs/ods-components-form-elements-form-field--documentation) label (and/or an helper) to clearly inform what information the user should type.          |
` }
</Markdown>

<Markdown className="documentation-dont-table">
  { `
| ❌ Don't                                                                                                                                                                          |
|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| - Use **Textarea** for shorter text.                                                                                                                                             |
| - Use a placeholder as replacement for a [**Form Field**](?path=/docs/ods-components-form-elements-form-field--documentation) label.                                                      |
` }
</Markdown>

## Placement

**Textarea** is used in forms where the expected user input text would be long.

## Behavior

**Textarea** can be hovered, focused, disabled or read only.

**When disabled**, the component cannot be focused nor clicked.

When the user types an amount of **text longer than Textarea** container itself, a scrollbar is displayed.

**Textarea** is optionally resizable: user can expand its container.

An event is triggered when the **Textarea** value changes, and on user focusing in and out of the component.

## Variation

N/A

## Accessibility

`Tab` allows to focus on **Textarea**.
