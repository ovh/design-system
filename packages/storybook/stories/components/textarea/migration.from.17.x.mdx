import { Badge } from '@ovhcloud/ods-react';
import { Meta, Source } from '@storybook/blocks';
import * as TextareaStories from './textarea.stories';
import { Heading } from '../../../src/components/heading/Heading';

<Meta of={ TextareaStories } name="Migration v17 to v18" />

<Heading label="Textarea - migrate from v17 to v18" level={ 1 } />

<Heading label="Attributes changes" level={ 2 } />

`color ` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

Form components does not provide color variant anymore.
You can use custom CSS if you need to customize the component.

`contrasted` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

Form components does not provide color variant anymore.
You can use custom CSS if you need to customize the component.

`disabled` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `isDisabled` attribute to obtain the same behavior.

`error` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `hasError` attribute to obtain the same behavior.

`errorStateControl` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

Was not relevant for few versions already.

`formControl` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

Was not relevant for few versions already.

`inline` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

Textarea is now `inline-block` by default.
You can add some style directly on the component if you want to override that.

`readonly` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `isReadonly` attribute to obtain the same behavior.

`required` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `isRequired` attribute to obtain the same behavior.

`resizable` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `isResizable` attribute to obtain the same behavior.

`size ` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

Form components does not provide size variant anymore.
You can use custom CSS if you need to customize the component.

`spellcheck` <Badge color="information" size="sm">Updated</Badge>

Has been updated.

You can use the new `hasSpellcheck` attribute to obtain the same behavior.

`textAreaId ` <Badge color="critical" size="sm">Removed</Badge>

Has been removed.

This was used to manage a default aria value which is not set by default.

<Heading label="Migration examples" level={ 2 } />

Disabled textarea:

<Source code={`
<osds-textarea disabled>
</osds-textarea>

<!-- is now -->

<ods-textarea is-disabled>
</ods-textarea>
`}
        dark="true"
        language="html" />

Error textarea:

<Source code={`
<osds-textarea error>
</osds-textarea>

<!-- is now -->

<ods-textarea has-error>
</ods-textarea>
`}
        dark="true"
        language="html" />

Inline textarea:

<Source code={`
<osds-textarea inline>
</osds-textarea>

<!-- is now -->

<ods-textarea>
</ods-textarea>
`}
        dark="true"
        language="html" />

Readonly textarea:

<Source code={`
<osds-textarea readonly>
</osds-textarea>

<!-- is now -->

<ods-textarea is-readonly>
</ods-textarea>
`}
        dark="true"
        language="html" />

Required textarea:

<Source code={`
<osds-textarea required>
</osds-textarea>

<!-- is now -->

<ods-textarea is-required>
</ods-textarea>
`}
        dark="true"
        language="html" />

Resizable textarea:

<Source code={`
<osds-textarea resizable>
</osds-textarea>

<!-- is now -->

<ods-textarea is-resizable>
</ods-textarea>
`}
        dark="true"
        language="html" />

Spellcheck textarea:

<Source code={`
<osds-textarea spellcheck>
</osds-textarea>

<!-- is now -->

<ods-textarea has-spellcheck>
</ods-textarea>
`}
        dark="true"
        language="html" />
