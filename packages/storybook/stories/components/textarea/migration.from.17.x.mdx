import { Meta } from '@storybook/blocks';
import * as TextareaStories from './textarea.stories';

<Meta of={ TextareaStories } name='Migration From 17.x' />

# Textarea - migrate from v17 to v18
----

## Attributes changes

`color ` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Form components does not provide color variant anymore.
You can use custom CSS if you need to customize the component.

`contrasted` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Form components does not provide color variant anymore.
You can use custom CSS if you need to customize the component.

`disabled` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `isDisabled` attribute to obtain the same behavior.

`error` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `hasError` attribute to obtain the same behavior.

`errorStateControl` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Was not relevant for few versions already.

`formControl` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Was not relevant for few versions already.

`inline` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Textarea is now `inline-block` by default.
You can add some style directly on the component if you want to override that.

`readonly` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `isReadonly` attribute to obtain the same behavior.

`required` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `isRequired` attribute to obtain the same behavior.

`resizable` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `isResizable` attribute to obtain the same behavior.

`size ` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

Form components does not provide size variant anymore.
You can use custom CSS if you need to customize the component.

`spellcheck` <ods-badge size="sm" icon="circle-info" label="Updated"></ods-badge>

Has been updated.

You can use the new `hasSpellcheck` attribute to obtain the same behavior.

`textAreaId ` <ods-badge size="sm" color="critical" icon="circle-xmark" label="Removed"></ods-badge>

Has been removed.

This was used to manage a default aria value which is not set by default.

## Migration examples

Disabled textarea:
```html
<osds-textarea disabled>
</osds-textarea>

<!-- is now -->

<ods-textarea is-disabled>
</ods-textarea>
```

Error textarea:
```html
<osds-textarea error>
</osds-textarea>

<!-- is now -->

<ods-textarea has-error>
</ods-textarea>
```

Inline textarea:
```html
<osds-textarea inline>
</osds-textarea>

<!-- is now -->

<ods-textarea>
</ods-textarea>
```

Readonly textarea:
```html
<osds-textarea readonly>
</osds-textarea>

<!-- is now -->

<ods-textarea is-readonly>
</ods-textarea>
```

Required textarea:
```html
<osds-textarea required>
</osds-textarea>

<!-- is now -->

<ods-textarea is-required>
</ods-textarea>
```

Resizable textarea:
```html
<osds-textarea resizable>
</osds-textarea>

<!-- is now -->

<ods-textarea is-resizable>
</ods-textarea>
```

Spellcheck textarea:
```html
<osds-textarea spellcheck>
</osds-textarea>

<!-- is now -->

<ods-textarea has-spellcheck>
</ods-textarea>
```
