import { Canvas, Meta, Markdown } from '@storybook/blocks';
import SpecificationsTimepicker from '@ovhcloud/ods-components/src/components/timepicker/documentation/spec.md?raw';
import { Banner } from '../../banner';
import { DocNavigator } from '../../doc-navigator';
import * as TimepickerStories from './timepicker.stories';
import { LINK_ID } from '../../zeroheight';

<Meta of={ TimepickerStories } name='Documentation' />

<Banner of={ TimepickerStories } />

# Overview

A Timepicker component is used to allow users to select an hour and a timezone.

If timezones are set, but not the currentTimezone, the currentTimezone is automatically set with the timezone of the browser.

<Canvas of={ TimepickerStories.Default } sourceState='none' />

<DocNavigator of={ TimepickerStories } linkId={ LINK_ID.TIMEPICKER } />

<Markdown>{ SpecificationsTimepicker }</Markdown>

# Style customization

You can add your own style on the timepicker element using the part `input` or `select`.

Custom timepicker css:

<Canvas of={ TimepickerStories.CustomCSS } sourceState='shown'/>

# Examples

## Default

<Canvas of={ TimepickerStories.Default } sourceState='shown' />

## Disabled

<Canvas of={ TimepickerStories.Disabled } sourceState='shown' />

## Error

<Canvas of={ TimepickerStories.Error } sourceState='shown' />

## Readonly

<Canvas of={ TimepickerStories.Readonly } sourceState='shown' />

## Timezones

<Canvas of={ TimepickerStories.Timezones } sourceState='shown' />

## WithSeconds

<Canvas of={ TimepickerStories.WithSeconds } sourceState='shown' />




