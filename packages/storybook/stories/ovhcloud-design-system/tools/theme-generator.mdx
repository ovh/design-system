import { Meta, Source } from '@storybook/blocks';
import { Heading } from '../../../src/components/heading/Heading';
import { NoToC } from '../../../src/components/noToC/NoToC';
import { ThemeGenerator } from '../../../src/components/themeGenerator/ThemeGenerator';
import packageJson from '../../../package.json';

<Meta
  tags={ ['beta'] }
  title="OVHcloud Design System/Tools/Theme Generator" />

<NoToC />

<Heading label="Theme Generator" level={ 1 } />

This is a live theme generator where you can create and customize themes for the ODS React library.

It allows you to experiment with different color schemes, typography, and styling options to create custom themes that match your brand or application requirements.

<ThemeGenerator />
