import { OdsBadge } from '@ovhcloud/ods-components/react';
import { Meta, Source } from '@storybook/blocks';
import * as RadioStories from './radio.stories';
import { Heading } from '../../../src/components/heading/Heading';

<Meta of={ RadioStories } name="Migration From 17.x" />

<Heading label="Radio - migrate from v17 to v18" level={ 1 } />

<Heading label="Attributes changes Radio" level={ 2 } />

`checked` <OdsBadge color="information" label="Updated" size="sm" />

Has been updated.

You can use the new `isChecked` attribute to obtain the same behavior.

`checking` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

This design feature has been removed in order to match the new styling of the component.

`disabled` <OdsBadge color="information" label="Updated" size="sm" />

Has been updated.

You can use the new `isDisabled` attribute to obtain the same behavior.

`label` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

Radio can now use HTML Label. Like in the example.

`afterSave` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

We don't think this kind of callback was necessary anymore.

`beforeSave` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

We don't think this kind of callback was necessary anymore.

`save` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

We don't think this kind of callback was necessary anymore.

<Heading label="Attributes changes Radio group" level={ 2 } />

`required` <OdsBadge color="information" label="Updated" size="sm" />

Has been updated.

You can use the new `isRequired` attribute to obtain the same behavior.

<Heading label="Attributes changes Radio button" level={ 2 } />

`color` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

This design feature has been removed in order to match the new styling of the component.

You can use the CSS to obtain the same rendering.

`hasFocus` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

You have now an event to know when the component is focused.

`interactive` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

This design feature has been removed in order to match the new styling of the component.

`size` <OdsBadge color="critical" label="Removed" size="sm" />

Has been removed.

You can use the CSS to obtain the same rendering.

<Heading label="Migration examples" level={ 2 } />

Disabled radio:

<Source code={`
<osds-radio disabled>
  <osds-radio-button color='primary' size='sm'></osds-radio-button>
</osds-radio>

<!-- is now -->

<ods-radio is-disabled></ods-radio>
`}
        dark="true"
        language="html" />

Label radio:

<Source code={`
<osds-radio-button>
  <span slot='end'>Radio label</span>
</osds-radio-button>

<!-- is now -->

<ods-radio input-id="migration-label" name="name" value="migration"></ods-radio>
<label for="migration-label">Radio label</label>
`}
        dark="true"
        language="html" />

Group radio:

<Source code={`
<osds-radio-group>
    <osds-radio value="A">
        <osds-radio-button color='primary' size='sm'></osds-radio-button>
    </osds-radio>
    <osds-radio value="B">
        <osds-radio-button color='primary' size='sm'></osds-radio-button>
    </osds-radio>
    <osds-radio value="C">
        <osds-radio-button color='primary' size='sm'></osds-radio-button>
    </osds-radio>
</osds-radio-group>

<!-- is now -->

<ods-radio value="A" name="group-radio"></ods-radio>
<ods-radio value="B" name="group-radio"></ods-radio>
<ods-radio value="C" name="group-radio"></ods-radio>
`}
        dark="true"
        language="html" />
