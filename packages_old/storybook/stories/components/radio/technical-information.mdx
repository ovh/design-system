import { Canvas, Meta } from '@storybook/blocks';
import SpecificationsRadio from '@ovhcloud/ods-components/src/components/radio/documentation/custom-elements.json';
import { Banner } from '../../../src/components/banner/Banner';
import { Heading } from '../../../src/components/heading/Heading';
import { TechnicalSpecification } from '../../../src/components/technicalSpecification/TechnicalSpecification';
import { hideValidityStateSource } from '../../../src/components/validityState/validityState';
import * as RadioStories from './radio.stories';
import { Radio as FormFieldRadio } from '../form-field/form-field.stories';

<Meta of={ RadioStories } name="Technical information" />

<Banner of={ RadioStories } />

<Heading label="Overview" level={ 2 } />

**Radio** are used when, facing a list of two or more options, the user must select only one.

The previous components `radio`, `radio-button` and `radio-group` have now been included in `radio`

The behavior of the old `radio-group` is now automatic, giving the radio the same name as the native implementation.

There is no longer a `radio` component to make clickable Element.

<Canvas of={ RadioStories.Checked } sourceState="none" />

<TechnicalSpecification data={ SpecificationsRadio } />

<Heading label="Style customization" level={ 2 } />

You can add your own style on the radio element using the part `radio`.

Custom CSS:

<Canvas of={ RadioStories.CustomCSS } sourceState="shown" />

<Heading label="Examples" level={ 2 } />

<Heading label="Default" level={ 3 } />

<Canvas of={ RadioStories.Default } sourceState="shown" />

<Heading label="Alignment" level={ 3 } />

The radio component may not be vertically aligned by default, depending on the content following it.
You'll need to manage this with CSS on parent element.

<Canvas of={ RadioStories.Alignment } sourceState="shown" />

<Heading label="Checked" level={ 3 } />

<Canvas of={ RadioStories.Checked } sourceState="shown" />

<Heading label="Disabled" level={ 3 } />

<Canvas of={ RadioStories.Disabled } sourceState="shown" />

<Heading label="Disabled Checked" level={ 3 } />

<Canvas of={ RadioStories.DisabledChecked } sourceState="shown" />

<Heading label="Error" level={ 3 } />

<Canvas of={ RadioStories.Error } sourceState="shown" />

<Heading label="Group" level={ 3 } />

<Canvas of={ RadioStories.Group } sourceState="shown" />

<Heading label="Group checked" level={ 3 } />

The attribute `is-checked` works like the native implementation, defining the default checked radio in a group.

<Canvas of={ RadioStories.GroupIsChecked } sourceState="shown" />

<Heading label="InputId" level={ 3 } />

The `id` is encapsulated by the `input-id` attribute. This way you can link a label (attribute `for`) to the input.

<Canvas of={ RadioStories.InputId } sourceState="shown" />

<Heading label="Validity State" level={ 3 } />

<Canvas of={ RadioStories.ValidityState } source={{ transform: hideValidityStateSource }} sourceState="shown" />

<Heading label="Included in a Form Field" level={ 3 } />

<Canvas of={ FormFieldRadio } sourceState="shown" />
