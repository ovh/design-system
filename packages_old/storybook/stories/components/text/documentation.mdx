import { Canvas, Meta } from '@storybook/blocks';
import * as TextStories from './text.stories';
import { Banner } from '../../../src/components/banner/Banner';
import { BestPractices } from '../../../src/components/bestPractices/BestPractices';
import { Heading } from '../../../src/components/heading/Heading';
import { IdentityCard } from '../../../src/components/identityCard/IdentityCard';
import { ATOMIC_TYPE } from '../../../src/constants/atomicDesign';

<Meta of={ TextStories } name="Documentation" />

<Banner of={ TextStories } />

_The **Text** component is used to display and style text content within an application._

<Canvas of={ TextStories.Overview } sourceState="none" />

<Heading label="Overview" level={ 2 } />

<IdentityCard aliases={ ['Paragraph'] }
              atomicType={ ATOMIC_TYPE.quark }
              figmaLink="https://www.figma.com/design/9jDDTcR4a9jPRFcdjawAlf/ODS---UI-Kit?node-id=172-12061"
              githubUrl="https://github.com/ovh/design-system/tree/master/packages/ods/src/components/text"
              name="Text"
              relatedComponents={ [] }>
  The **Text** component is a fundamental UI element for displaying text content.

  It supports various presets of styles, sizes, and weights to cater to different textual content requirements such as
  paragraphs, headings, and inline text.

  The component can be extra customized to fit the design and branding needs of the application.
</IdentityCard>

<Heading label="Anatomy" level={ 2 } />

![Component anatomy](components/text/anatomy.png "Component anatomy")

1. **Main content**

<Heading label="Usage" level={ 2 } />

**Text** component is used to standardize the text in a specific style without worrying about browser compatibility issues.

It brings clarity in a page by displaying content as clearly and efficiently as possible.

All texts in a **Text** component inherit from a common theme system.

<Heading label="Dos & Don'ts" level={ 3 } />

<BestPractices
  donts={[
    '- Skip heading levels',
  ]}
  dos={[
    '- Use a consistent heading hierarchy',
  ]}
/>

<Heading label="Placement" level={ 2 } />

**Text** can be used everywhere in a page as long as levels and heading hierarchy are respected.

It can stand alone or be included in other components depending on the usage.

<Heading label="Behavior" level={ 2 } />

Based on its informational manner, the **Text** default behavior is being read-only.

The user can only select its content if needed.

<Heading label="Variation" level={ 2 } />

N/A

<Heading label="Accessibility" level={ 2 } />

**Text** content is readable by screen readers.
