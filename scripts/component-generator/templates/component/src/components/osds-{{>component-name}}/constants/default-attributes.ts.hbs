import type { Ods{{> componentName }}Attribute } from '../interfaces/attributes';

const DEFAULT_ATTRIBUTE: Ods{{> componentName }}Attribute = Object.freeze({
  dummy: '',
});

export {
  DEFAULT_ATTRIBUTE,
};
