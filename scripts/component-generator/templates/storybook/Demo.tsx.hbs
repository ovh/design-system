import { {{> ComponentName }}, type {{> ComponentName }}Prop } from '@ovhcloud/ods-react';
import React from 'react';

export default (prop: {{> ComponentName }}Prop) => (
  <{{> ComponentName }} { ...prop } />
);
